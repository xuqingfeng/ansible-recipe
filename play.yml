---
  - hosts: all
    remote_user: root
    vars_files:
      - vars.yml
    roles:
      - {role: git, tags: ["git"]}
      - {role: golang, tags: ["golang"]}
      - {role: golang-bin, tags: ["golang-bin"]}
      - {role: influxDB}
      - {role: kapacitor}
      - {role: tmux, tags: ["tmux"]}
      - {role: zsh, tags: ["zsh"]}
