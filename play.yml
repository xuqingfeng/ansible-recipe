---
  - hosts: all
    remote_user: root
    roles:
      - {role: git, tags: ["git"]}
      - {role: tmux, tags: ["tmux"]}
      - {role: zsh, tags: ["zsh"]}
      - {role: golang, tags: ["golang"]}
      - {role: golang-bin, tags: ["golang-bin"]}
